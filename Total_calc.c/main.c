#include <stdio.h>
#include <stdlib.h>

int main()
{  int total;
   total = 8;
   printf("pre pre totl %d",total +5);

   printf("\npre total %d",total);

   printf("\nold total %d",total++);

   printf("\nnew total %d",total);

   printf("\n newest total %d",++total);

   printf("\nfinal total %d", total);
    return 0;
}
